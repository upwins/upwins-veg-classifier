# Pinned so a fresh clone reproduces the tutorial environment.
#
# IMPORTANT: a pickled StandardScaler and a saved .keras model are sensitive
# to the versions they were created with. These pins match the NVIDIA
# `nvcr.io/nvidia/tensorflow:24.12-tf2-py3` container the project was built in.
# If you retrain in a different environment, run `pip freeze` there and update
# tensorflow / scikit-learn / numpy to match, or the example model may not load.

tensorflow==2.17.0
numpy==1.26.4
scikit-learn==1.5.1
pandas==2.2.2
scipy==1.13.1
spectral==0.23.1
matplotlib==3.9.2
seaborn==0.13.2
PyYAML==6.0.2

# Optional: only needed to refresh the spectral library from MongoDB
# (data_source.reload_from_mongodb: true).
pymongo==4.8.0
python-dotenv==1.0.1
