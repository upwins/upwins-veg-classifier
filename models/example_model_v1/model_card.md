# Model Card — example_model_v1

The model card for this bundle lives at **[`docs/model_card.md`](../../docs/model_card.md)**.

It is kept in one place on purpose: this file used to be a byte-identical copy,
and two copies drift. Edit `docs/model_card.md` and nothing else.
