# Sample data placeholder

Drop a **small** example here so the notebooks run from a fresh clone:

- A spatially cropped ENVI reflectance cube (e.g. 200x200) named
  `raw_0_ref` + `raw_0_ref.hdr`.
- A few labeled ROI `.pkl` files (or point `config.yaml:paths.roi_dir` at
  `data/rois_labeled/`).

Keep the whole committed sample well under ~50 MB so cloning stays fast. Use
your full data for the recorded tutorial by editing `config.yaml`.
